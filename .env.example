# External backend API URL — server-side only (never exposed to client)
API_URL=http://localhost:3001/api

# DEV ONLY: set to true in .env.local to bypass auth guards (never set in production)
# NEXT_PUBLIC_AUTH_BYPASS=true
